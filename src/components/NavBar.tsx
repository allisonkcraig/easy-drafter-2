import React from 'react';
import '../style/NavBar.css';

const NavBar: React.FC = () => {
  return (
    <nav className="navbar">
      <span className="navbar-logo">Easy Drafter 2.0</span>
      {/* TODO add nav items and links */}
    </nav>
  );
};

export default NavBar;
