import React from 'react';

const NavBar: React.FC = () => {
  return (
    <div>
      {/* TODO: on this page we will gather two measurements to use to determine base size */}
    </div>
  );
};

export default NavBar;
